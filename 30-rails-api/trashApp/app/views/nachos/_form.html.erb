<%= form_with(model: nacho, local: true) do |form| %>
  <% if nacho.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nacho.errors.count, "error") %> prohibited this nacho from being saved:</h2>

      <ul>
      <% nacho.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :tasty %>
    <%= form.check_box :tasty %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
